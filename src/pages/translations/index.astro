---
import { getCollection } from "astro:content";
import BaseLayout from "../../layouts/BaseLayout.astro";
import {
  filterPublished,
  formatPoetEnglishCredit,
  sortByOrderThenTitle,
  withBase,
  entrySlug,
} from "../../lib/content";

const poems = sortByOrderThenTitle(filterPublished(await getCollection("poems")));
---

<BaseLayout title="תרגומים" current="translations">
  <div class="container">
    <h1 class="page-title">תרגומים</h1>
    <p class="page-intro">תרגומי שירה — המקור והתרגום זה לצד זה.</p>
    {
      poems.length === 0 ? (
        <p class="item-meta">עדיין לא פורסמו תרגומים.</p>
      ) : (
        <ul class="section-list">
          {poems.map((poem) => (
            <li>
              <a href={withBase(`translations/${entrySlug(poem.id)}/`)}>
                {poem.data.title}
              </a>
              <span class="item-meta">
                {poem.data.author}
                {
                  poem.data.authorEn && (
                    <>
                      {" · "}
                      <span dir="ltr" lang="en">
                        {formatPoetEnglishCredit(
                          poem.data.authorEn,
                          poem.data.authorBirth,
                          poem.data.authorDeath,
                        )}
                      </span>
                    </>
                  )
                }
              </span>
            </li>
          ))}
        </ul>
      )
    }
  </div>
</BaseLayout>
